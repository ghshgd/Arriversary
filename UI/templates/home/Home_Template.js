import HomeScreen from '../../pages/home/HomeScreen';
import {getUserInfo} from '../../../src/firebase/firebase';
import {useEffect} from 'react';
export default function Home_Template({navigation}) {
  return <HomeScreen />;
}
